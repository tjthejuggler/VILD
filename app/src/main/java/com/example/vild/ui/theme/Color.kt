package com.example.vild.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dream palette ─────────────────────────────────────────────────────────────
// Deep night-sky voids, indigo nebulae, aurora teals and moonlit lavender.
// Designed to feel like the space between waking and sleeping.

/** Near-black with a violet cast — the base void of every screen. */
val Void = Color(0xFF060312)
/** Deepest nebula layer. */
val DeepIndigo = Color(0xFF140B33)
/** Mid-nebula indigo. */
val Indigo = Color(0xFF2A1B5E)
/** Luminous violet — primary accent. */
val Violet = Color(0xFF8A7CFF)
/** Soft moonlit lavender — secondary accent / dreamy text. */
val MoonLavender = Color(0xFFC4B5FD)
/** Aurora teal — "confirmed / alive" accent. */
val AuroraTeal = Color(0xFF6EE7D8)
/** Faded dream pink — tertiary accent. */
val DreamPink = Color(0xFFF0A6CA)
/** Warm distant star gold. */
val StarGold = Color(0xFFF5D491)
/** Muted lavender-grey for secondary text. */
val Mist = Color(0xFF9D94C7)

// Translucent glass tints used by cards floating over the dream background.
val GlassSurface = Color(0x22120B2E)
val GlassBorder = Color(0x40C4B5FD)

// ── Legacy greyscale palette (kept for older components) ─────────────────────
val White = Color(0xFFFFFFFF)
val Grey95 = Color(0xFFF2F2F2)
val Grey90 = Color(0xFFE6E6E6)
val Grey80 = Color(0xFFCCCCCC)
val Grey70 = Color(0xFFB3B3B3)
val Grey60 = Color(0xFF999999)
val Grey50 = Color(0xFF808080)
val Grey40 = Color(0xFF666666)
val Grey30 = Color(0xFF4D4D4D)
val Grey20 = Color(0xFF333333)
val Grey15 = Color(0xFF262626)
val Grey10 = Color(0xFF1A1A1A)
val Black = Color(0xFF000000)
